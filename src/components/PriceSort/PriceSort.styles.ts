import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  sortContainer: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
  },
  sortArrowImage: {
    height: 15,
    width: 15,
    resizeMode: 'contain',
  },
});

export default styles;
